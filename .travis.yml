language: go

go_import_path: github.com/shoebox/go-playstore-publisher

go:
  - 1.13.x

before_install:
  - go get -t -v ./...

install:
  - export PATH=$PATH:$HOME/gopath/bin
  - go get -v -t ./...

script:
  - go test goplaypublisher/playpublisher -coverprofile=coverage.txt -covermode=atomic

after_success:
  - bash <(curl -s https://codecov.io/bash)

